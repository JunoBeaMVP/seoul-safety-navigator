# 보안 정책

## 지원 범위

현재 유지되는 기본 브랜치의 최신 버전에 대해 보안 문제를 검토합니다. 긴급한 안전 안내 오류, 인증 우회, 권한 없는 데이터 접근, 비밀값 노출은 높은 우선순위로 다룹니다.

## 취약점 신고

**공개 Issue에 취약점 재현 절차, API 키, 사용자 위치, 개인정보를 올리지 마세요.** 저장소 관리자에게 비공개 연락 수단으로 다음 최소 정보를 전달해 주세요.

| 항목 | 포함할 내용 |
| --- | --- |
| 영향 | 영향을 받는 기능과 예상되는 보안·안전상 결과 |
| 재현 | 비밀값·개인정보를 제거한 최소 재현 절차 |
| 환경 | 브라우저, 기기, 배포 버전, 발생 시간대 |
| 완화 | 이미 적용한 임시 조치가 있으면 그 내용 |

접수 후 재현 가능성, 영향 범위, 수정 우선순위를 검토합니다. 취약점이 확인되면 공개 전에 수정·검증·배포 순서로 대응합니다.

## API 키와 운영 비밀값

다음 값은 서버 환경변수 또는 배포 플랫폼의 비밀값 저장소에서만 관리합니다.

| 변수명 | 용도 | 금지 사항 |
| --- | --- | --- |
| `KMA_SERVICE_KEY` | 기상청 특보·실황 | 클라이언트 코드·문서·Issue·로그에 값 기록 |
| `SEOUL_OPEN_API_KEY` | 서울시 대피소 | 커밋·프런트엔드 번들·공개 URL에 포함 |
| `OPENROUTE_SERVICE_KEY` | 도보 경로 | 브라우저 요청·테스트 출력·스크린샷에 노출 |

실수로 비밀값을 커밋했을 가능성이 있으면 즉시 해당 키를 폐기·재발급하고, 저장소 이력과 배포 로그를 점검해야 합니다. GitHub Actions를 사용할 경우에는 GitHub Secrets를 사용하고 로그에 값을 출력하지 않아야 합니다.[^github-secrets]

## 위치 정보와 안전 데이터

브라우저 위치는 대피소 검색과 경로 계산에 필요한 범위에서만 사용합니다. 이 앱은 사용자의 정밀 위치를 데이터베이스에 영구 저장하지 않도록 설계합니다. 공개 저장소의 이슈·로그·스크린샷에는 위치 좌표와 개인 식별 정보를 포함하지 마세요.

[^github-secrets]: [GitHub Docs, Using secrets in GitHub Actions](https://docs.github.com/en/actions/security-guides/using-secrets-in-github-actions)
